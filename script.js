// Result of the form
function result (form) {
    alert (
        "These are your details,"
        + "\nName : " + form.name.value
        + "\nPassword : " + form.password.value
        + "\nRecovery Email : " + form.email.value
    );

    loacation.reaload();
};